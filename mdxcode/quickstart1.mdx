import Code from '../components/Code/Code';

<Code fileNames={['query.ts', 'query.sql']}>
```typescript
const result: User[] = await db.select().from(users);

result[0].id;
result[0].name;
```
```sql
select * from 'users';
```
</Code>
