// show simple refine for 1 table
// show refine works with multiple tables
// show refine works with multiple tables that are connected

// show all the available functions and how they work and what generate
// show weighted random