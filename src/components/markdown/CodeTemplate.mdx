
import CodeWithProps from "@/components/markdown/CodeWithProps.astro";

<CodeWithProps name={props.name}>
    ```ts
    const $name$ = await db.query.$name$.$name$({
        with: {
            posts: "$name$"
        },
    });
    ```
</CodeWithProps>